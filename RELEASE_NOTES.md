# 📋 Release Notes — hk-racing-analyzer

> 记录每次重要优化和变更，便于追踪 Skill 演进历史。

---

## v1.5.0 — 2026-04-07

### 💰 智能投注推荐模块

**新增**：`scripts/betting.py` — 独立投注推荐模块，根据概率分布 + 赔率性价比自动判断每场最优玩法。

#### 核心功能

**1. 场型判断 + 最优玩法推荐**（`determine_bet_type()`）

每场只推荐一种最佳玩法，决策逻辑：

| 场型 | 条件 | 推荐玩法 |
|------|------|---------|
| A：高置信独赢场 | top1 概率 ≥ 28% 且 value_index ≥ 1.05 且 赔率 ≤ 6 | WIN |
| B：双强对决场 | top1+top2 概率和 ≥ 55% 且 概率差 < 15% | Q（连赢）|
| C：标准三强场 | top3 概率和 ≥ 60% | TRIO（三重彩）|
| D：开放冷门场 | 以上不满足 | PLACE（位置）|

降级规则：WIN 赔率 < 1.5 → 降为 PLACE；Q 两头赔率均 > 12 → 降为 TRIO。

**2. 价值指数计算**（`compute_value_index()`）
- value_index = model_prob / (1/odds × 0.92)
- > 1.2 → 🔥 超值  |  0.8~1.2 → 普通  |  < 0.8 → ⚠️ 市场高估

**3. 冷门建议接口**（`get_longshot_tip()`）
- 独立接口，不进报告，供实时问答使用
- 条件：赔率 > 8 且 value_index > 1.3，longshot_alert 标记优先

**4. 回测命中验证**（`check_bet_hit()`）
- WIN：推荐马号 == 冠军
- PLACE：推荐马号 in 前3
- Q：推荐两匹 == 实际前2（顺序不限）
- TRIO：推荐三匹 == 实际前3（顺序不限）

#### 预测报告变更（`output.py`）

- 原有「投注建议」区域替换为「最推荐投注方案」
- 每场末尾输出一行：`💰 推荐投注：[TRIO] 三重彩 #3 #7 #11 | 概率覆盖 71.2% | 场型：标准三强场`

#### 回测变更（`daily_scheduler.py`）

- 预测存档新增 `betting_recommendation` 字段
- 回测逐场验证推荐玩法是否命中，新增 `bet_result` 字段
- 进化报告新增「💰 投注推荐回测统计」板块（按玩法分命中率统计）
- 进化建议新增：投注推荐命中率偏低时（≥3 场样本，命中率 < 25%）触发优化建议

### 🐛 Bug 修复：赛马日场地检测误判（v1.5.0）

**问题**：HKJC 旧版 API（`RaceCard.aspx`）的 `Venue` 参数有时被忽略，无论请求 `ST`（沙田）还是 `HV`（跑马地），均返回当天唯一有赛事的场地数据。导致脚本先检测沙田时，若当天实际只有跑马地有赛事，沙田请求会返回跑马地的数据，误判为沙田并短路跑马地检测。

**修复**：

1. `detect_race_day()` 增加场地验证：从返回 HTML 的分场链接中提取实际 `Racecourse=XX`，与请求的 `venue_code` 比对，不匹配则跳过
2. 排位表无赛事字符串增加 `"没有相关资料"` 匹配（原仅 `"没有赛事资料"`，HKJC 两种表述均有可能）

**影响**：所有依赖 `detect_race_day()` 的自动化任务（`--mode predict` / `--mode backtest`）均受益

---

## v1.4.12 — 2026-04-07

### 🧬 基于 2026-04-06 沙田回测数据的深度进化（4条优化）

**背景**：v1.4.11 在 2026-04-06 沙田 11 场回测中精度为：独赢 27.3%（3/11），前3 45.5%。
深度分析后发现 3 个系统性问题，本版本同时应用全部 4 条进化建议。

---

#### 建议A — 修复 opening_odds（`main.py` + `daily_scheduler.py`）

**问题**：`opening_odds` 全场硬编码为 `None`，`score_odds_drift()` 全部返回中性 50，
18% 的 drift 权重实际完全失效（11/11 场冠军 drift_score=50）。

**修复**：预测时将当前抓取的赔率同时存为 `opening_odds`（赔率快照），
并在预测存档中新增 `predicted_odds_snapshot` 字段，
使下次预测或回测时可利用历史赔率快照进行真正的 drift 分析。

#### 建议B — tips_index 权重 6% → 4%（`config.py`）

**问题**：贴士指数与赔率高度共线——贴士值低的马正好是赔率热门，
`tips_index(6%) + odds_value(22%)` 实质在重复表达同一信息，人为拉高热门马概率。

**修复**：`tips_index` 权重从 6% 降至 4%，释放 2% 补充至 `history_same_venue`（15%→17%）。

| 维度 | v1.4.11 | v1.4.12 | 说明 |
|------|---------|---------|------|
| tips_index | 6% | **4%** | ↓ 降低共线重复信号 |
| history_same_venue | 15% | **17%** | ↑ 补充场地历史数据权重 |

#### 建议C — hist_same_condition 首胜奖励 +10 → +3（`scoring.py`）

**问题**：`wins>=1` 时 base=55，近3场有冠军再 +10=65，接近2胜马的 75 分，
"单胜马"被过度拉高，模型对首胜/单胜马有系统性高估偏差。

**修复**：近3场有冠军加成从 +10 改为 +3（65 → 58）。
分档含义更清晰：单胜=55-58，双胜=75+，差距合理。

#### 建议D — Softmax 温度动态化（`probability.py`）

**问题**：固定 T=4.0 对所有场次使用相同的概率分摊度，
在超悬殊场（大热门 vs 极大冷门）和均衡场（各马赔率相近）均不理想。

**修复**：新增 `dynamic_temperature(win_odds_map)` 函数，
根据场内赔率离散度（最大/最小赔率比）自动调整 T：

| 离散度 ratio | T | 场景 |
|-------------|---|------|
| > 20 | 6.0 | 超悬殊场（极大热门 vs 极大冷门） |
| > 10 | 5.0 | 大差异场（明显热门） |
| > 5 | 4.0 | 正常场（默认值不变） |
| ≤ 5 | 3.0 | 均衡场（各马赔率相近，无明显共识） |

无赔率数据时回退使用 `SOFTMAX_TEMPERATURE` 默认值（4.0）。

---

## v1.4.11 — 2026-04-05

### 🚀 赔率权重优化：即时市场信号主导预测

**背景**：v1.4.10 修复了赔率抓取 Bug（Playwright DOM 提取），实盘赔率数据现已可靠可用。
用户要求提高即时赔率的权重占比，因为它反映市场共识，是赛前最高质量的预测信号。

---

#### 1. 权重重新分配

| 维度 | v1.4.x | v1.4.11 | 变化 | 说明 |
|------|--------|--------|------|------|
| **赔率综合评分** | 15% | **22%** | **+7%** | 独赢20档精细评分 + 位置赔率加成 + 隐含胜率融合 |
| **赔率走势** | 15% | **18%** | **+3%** | 市场资金流向信号更受重视 |
| 同距离同场地历史 | 16% | **13%** | -3% | 降低历史数据权重（补充赔率共识） |
| 同场地历史 | 18% | **15%** | -3% | 同上 |
| 配速指数 | 10% | **8%** | -2% | 待配速实测数据接入后恢复 |
| 外部专家预测 | 4% | **0%** | -4% | 信息来源不稳定，移除 |
| 练马师 | 4% | **3%** | -1% | 次要因素 |
| 骑师 | 5% | **4%** | -1% | 次要因素 |
| 档位 | 5% | **4%** | -1% | 次要因素 |
| 班次适配 | 8% | **7%** | -1% | 次要因素 |
| **赔率系合计** | **30%** | **40%** | **+10%** | 赔率正式成为预测主导信号 |

#### 2. 赔率评分函数增强（scoring.py）

- **`score_odds_value()`**：独赢赔率从 5 档扩展到 20 档（1.5 倍以下=98分起），并加入位置赔率加成（独赢 > 8 + 位置 < 3.5 → +4~7 分）
- **新增 `score_implied_probability()`**：将独赢赔率转换为隐含胜率（扣除 HKJC 约 8% 抽水），直接映射为 0-100 分（1.8 odds → 评分 47；13 odds → 评分 7），避免全场 max 标准化导致的 score gap 过大问题
- **`odds_market_score` 融合公式**：`独赢分档×0.4 + 隐含胜率×0.6`，再通过 cap=0.50 压缩，防止赔率信号 gap 压垮其他维度

#### 3. Softmax 参数调整

| 参数 | v1.4.x | v1.4.11 | 说明 |
|------|--------|--------|------|
| SOFTMAX_TEMPERATURE | 2.0 | **4.0** | 提高使概率分摊更平滑，避免赔率信号增强后 score gap 拉大导致的极端概率 |
| PROB_CAP | 0.50 | **0.88** | T=4.0 下正常情况不会触发 cap，保留极端情况保护 |

**效果**：1.8 倍超级大热门 → 预测 80.6%（赔率隐含胜率 51%，模型叠加同场地/贴士/骑师正向信号后提升至 80.6%，属于合理高置信度）

#### 4. 全场赔率数据注入（main.py）

- `fetch_race_odds()` 返回的独赢/位置赔率字典现已注入每匹马
- 新增 `place_odds`（位置赔率）和 `all_win_odds`（全场独赢赔率字典）字段
- 这些数据用于赔率评分和隐含胜率计算

#### 5. Skill 双语文档结构优化

**背景**：Skill 面向双语用户（中文为主，英文备用），需保持参考文件与 SKILL 版本引用一致。

- **精简 SKILL.md / SKILL_EN.md**：移除冗长的详细流程代码块（报告模板/投注逻辑/对比/赛果），改为引用 `references/workflow.md`
  - SKILL.md：~432行 → **156行**（↓ 64%）
  - SKILL_EN.md：~427行 → **186行**（↓ 56%）
- **新建 `references/workflow.md`**：将详细工作流程外置，包含投注逻辑、CLI用法、马匹对比、赛果查询完整代码示例（~216行）
- **删除 `references/analysis_weights_en.md`（旧版）**：旧版权重数据已过时（v1.4.x），且 SKILL_EN.md 实际引用的是 `analysis_weights.md`（中文），该文件孤立有误导风险
- **恢复 `references/analysis_weights_en.md`**：根据 v1.4.11 精准翻译重建，SKILL_EN.md 同步更新引用路径
- 保留 `references/expert_sources_en.md`、`references/hkjc_urls_en.md`：翻译准确，无过时数据

---

## v1.4.10 — 2026-04-05

### 🐛 Bug 修复：赔率抓取失败（普通 HTTP 请求无法获取 JS 动态内容）

**症状**：即使 HKJC 赔率页面已开盘，分析报告中赔率仍显示 `None`，控制台输出 `⚠️ 赔率未开盘或抓取失败`。

**根因**：HKJC 投注赔率页面（`bet.hkjc.com/ch/racing/wp/...`）内容通过 JavaScript 动态渲染，`fetch_race_odds()` 使用普通 `urllib` HTTP 请求只能获取空壳 HTML，`parse_race_odds()` 无法提取任何数据。

**修复**：新增 `_fetch_odds_with_playwright()` 函数，使用 Playwright 从渲染后的 DOM 表格直接提取独赢/位置赔率（方案A：DOM 提取）。若 DOM 表格提取失败，自动回退到方案B：解析 Playwright 渲染后的 HTML（兼容现有 `parse_race_odds` 逻辑）。

---

## v1.4.9 — 2026-04-05

### 🐛 Bug 修复：批量预测存档全空（horses/top3 均为空列表）

**症状**：`daily_scheduler.py --mode predict` 运行成功，但存档中所有场次 `runners=0, top3=[]`。

**根因 1（`fetch.py`）**：`fetch_horse_history()` 调用 `fetch_url()` 时，若缓存命中且含 `parsed` 字段，`_cache_get()` 直接返回已解析的 dict（v1.4.8 新缓存策略）。该 dict 被传入 `parse_horse_history(html)` → `re.search(..., html)` 报 `TypeError: expected string or bytes-like object, got 'dict'` → 整场预测崩溃但被 `except Exception` 静默吞掉 → `top3=[]`。

**根因 2（`daily_scheduler.py`）**：`analyze_race.py --output json` 的输出字段为 `regular_horses`，但 `_run_single_prediction()` 使用 `data.get("horses", [])` 读取，始终拿到空列表。

**修复**：
- `fetch.py`：`fetch_horse_history()` 增加类型判断——若 `fetch_url()` 返回 dict（已解析），直接 return，不再调用 `parse_horse_history()`
- `daily_scheduler.py`：`_run_single_prediction()` 改为 `data.get("regular_horses") or data.get("horses", [])` 兼容两个字段名

---

## v1.4.8 — 2026-04-03

### 🚀 缓存系统优化（Zlib 压缩 + 结构化存储）

**背景**：原缓存直接存储原始 HTML（200KB/文件），长期运行后 `.cache/` 目录膨胀。

#### Zlib 压缩存储（`cache.py`）

- `_cache_set()` / `_cache_get()` 新增 Zlib 压缩/解压
- HTML 内容压缩率约 **70-80%**（200KB → 40-60KB）
- 新增 `_compress()` / `_decompress()` 辅助函数
- 旧版未压缩条目自动识别并兼容（`_compressed: false` 标记）

#### 结构化数据优先存储（`cache.py` + `fetch.py`）

- 新增 `parsed` 字段：直接存储解析后的结构化数据（如赛果 dict ~5KB vs 原始 HTML ~200KB）
- 读缓存时优先返回 `parsed`（若存在），避免回读时重复解析
- 涉及函数：
  - `fetch_horse_history()`：parse 后同步写回 `parsed` 字段
  - `fetch_race_results()`：parse 后同步写回 `parsed` 字段
  - `fetch_race_odds()`：赔率 dict 直接存为 `parsed`（不再存原始 HTML）
  - `_save_odds_cache()`：简化为调用 `_cache_set()`
- `_cache_get()` 新增 `cache_key` 参数，支持赔率等自定义 key 场景

**综合效果**：缓存空间预计减少 **80-90%**，且热路径（已缓存的赛果/赔率）不再重复解析 HTML。

---

### 🚀 投注赔率功能补全与优化

**背景**：v1.4.7 引入了投注赔率抓取框架，本版本补全解析逻辑并集成到主分析流程。

#### 投注赔率完整解析（`parse.py` → `fetch.py` → `main.py`）

**涉及文件**：`config.py`、`fetch.py`、`parse.py`、`main.py`

##### `parse_race_odds()` 函数（`parse.py`）
- 解析 bet.hkjc.com 投注赔率页面，支持 5 种投注方式：
  - **独赢 (Win)**：马号 → 赔率字典
  - **位置 (Place)**：马号 → 位置赔率字典
  - **连赢 (Quinella)**：组合字符串 → 赔率字典（如 `"1,2": 45.0`）
  - **三重彩 (Trio)**：三元组合 → 赔率字典
  - **位置Q (Quinella Place)**：二元组合 → 赔率字典
- 智能表头识别：动态匹配"馬號/馬号/Horse No"等列名
- 提取赔率更新时间戳
- 返回 `status: ok` / `unavailable`，赛前无赔率时正常返回空字典

##### `fetch_race_odds()` 函数（`fetch.py`）
- 独立缓存 key（`odds_{date}_{venue}_{race_no}`），避免与排位表缓存混淆
- 5 分钟 TTL，实时反映临场赔率变化
- `fetch_race_odds()` 在 main.py 中调用，将赔率数据注入每匹马的 `final_odds` 字段

##### `main.py` 集成
- 新增 `--odds` 参数（可选，控制是否抓取赔率）
- 赔率抓取后打印摘要：独赢/位置/连赢数量，热门前三马匹
- 支持 `--force-refresh` 强制刷新赔率缓存

**数据流向**：
```
bet.hkjc.com → fetch_race_odds() → parse_race_odds()
                                      ↓
                                  odds_data dict
                                      ↓
                              main.py 注入 → 每匹马的 final_odds
                                      ↓
                              scoring.py 评分函数使用
```

---

## v1.4.7 — 2026-04-03

### ✨ 新增功能

#### 投注赔率抓取（bet.hkjc.com）

- **涉及文件**：`config.py`、`fetch.py`、`parse.py`、`main.py`
- **功能**：从 HKJC 投注赔率页面抓取各投注方式赔率数据
- **URL 模板**：`https://bet.hkjc.com/ch/racing/wp/{YYYY-MM-DD}/{VENUE}/{RACE_NO}`
- **返回数据**：
  - `win` — 独赢赔率（dict，马号→赔率值）
  - `place` — 位置赔率
  - `quinella` — 连赢赔率
  - `trio` — 三重彩赔率
  - `quinella_place` — 位置Q赔率
  - `last_updated` — 更新时间
  - `status` — ok / unavailable
- **缓存 TTL**：5 分钟（临场实时变化）
- **注意事项**：赛前未开盘时返回 `status: unavailable`，赔率字段为空，均为正常现象

---

### 🐛 Bug 修复

#### `fetch_tips_index()` 致命 NameError

- **文件**：`fetch.py`
- **问题**：`tips = {}` 字典只在内部备用函数 `_fetch_tips_index_static()` 中初始化，主函数通过 Playwright 抓取时直接使用但从未赋值，导致每次贴士指数抓取都触发 `NameError`
- **修复**：在主函数 try 块前添加 `tips = {}` 初始化

#### `_time_weight()` 重复 import

- **文件**：`scoring.py`
- **问题**：`from datetime import datetime` 写在函数内部的 for 循环里，每次调用都重复查命名空间
- **修复**：将 `datetime` 与已有的 `date` 合并为一行提升到模块顶部 import

---

### 🧹 代码清理

#### 删除死代码：`score_tips_index()`

- **文件**：`scoring.py`
- **问题**：`score_tips_index()`（80-120 整数格式评分）从未被调用，实际使用 `score_tips_index_hkjc`（小数值格式），22 行定义纯属无效代码
- **修复**：删除整段函数定义

#### 删除废变量：`n_runners = 14`

- **文件**：`analyze.py`
- **问题**：`n_runners = 14` 声明后从未被引用，跑法判断逻辑完全不依赖此变量
- **修复**：删除该行

#### 消除重复定义：`_cache_path_local`

- **文件**：`fetch.py`
- **问题**：`fetch.py` 内定义了与 `cache.py` 中 `_cache_path()` 完全相同的函数（SHA256 哈希缓存路径），造成维护重复
- **修复**：删除 fetch.py 内的重复定义，改为从 `cache.py` 导入 `_cache_path`

---

### 📝 Skill 文档增强（借鉴 Punting Buddy）

#### 扩展触发关键词
- 新增："比较X号和Y号"、"今天赛果"等，识别马匹对比、赛果查询意图

#### Step 1 自然语言增强
- 日期：支持"今天"、"明天"、"明天下午"、"下周X"、"4月5日"等表述
- 场地：支持"今晚" → HV，"跑马地马场" → HV 等中文映射
- 意图识别：预测 / 马匹对比 / 赛果查询 三类

#### 报告新增投注风格定位
- 稳妥型（Top1 ≥ 35%）：优先独赢/位置
- 进取型（Top1 < 25%，均匀分布）：关注冷门、PQ/三重彩
- 纠结型（≥3匹各 20%+）：PQ 三组全包分散风险

#### 新增赛马对比功能文档
- 完整的 4 步对比流程：识别 → 抓取数据 → 逐维分析 → 输出对比报告

#### 新增当天赛果查询功能文档
- 数据来源：HKJC LocalResults.aspx
- 赛果表格输出格式规范

**同步更新**：`SKILL.md`（中文）+ `SKILL_EN.md`（英文）

#### 文档精简（减少幻觉风险）
- 辅助分析维度表：移除写死的权重列，改为引用 `weights.py` 动态计算
- 删掉 `综合评分 = 本地×90% + 专家×4%` 公式（代码实际用 softmax + 权重矩阵）
- 投注推荐逻辑：移除具体阈值数字（30%、18%、50% 等），改为"AI 自主判断"原则
- 投注风格定位表：移除具体阈值，改为引用 `classify_betting_style()` 函数
- 赛果查询：更新为使用 `fetch_race_results()` + `parse_race_results()`，替代 `web_fetch`
- 专家预测章节：两处重复内容合并，简化为一句话
- Resources 表：移除快速过时的行数近似值（~200/~140 等）
- CLI 用法：删除 Step 3 中的重复 bash 块，统一保留在"快速开始"

---

### 💻 代码层新增（Skill 无法替代的部分）

#### `output.py`：投注风格自动标注

- **新增** `classify_betting_style(sorted_horses)` 函数
- 根据 Top1 概率、Top1+Top2 和值、前三差值、高概率马数量 四个维度，自动判断稳妥型 / 进取型 / 纠结型
- 报告"💡 投注建议"前新增 `🎯 投注风格定位` 表格，一眼识别本场格局

#### `fetch.py + parse.py`：赛果查询模块

- **新增** `fetch_race_results(date, venue)` → 抓取 HKJC LocalResults.aspx，返回原始 HTML
- **新增** `parse_race_results(html)` → 解析全场赛果，返回按场次分组的结构化数据
  - 每匹马提取：名次、马号、马名、骑师、练马师、负磅、档位、头马距离、完成时间、独赢赔率
  - 支持多场次自动分组，按场次号排序
- **新增** `LOCAL_RESULTS_URL` 到 `config.py`

---

### 📌 待处理项（暂延）

| 优先级 | 类别 | 问题 | 暂延原因 |
|--------|------|------|----------|
| 🟡 中 | 缺陷 | `data_confidence` 置信度判断过于宽松 | 需结合回测数据验证 |
| 🟡 中 | 缺陷 | `current_rating` 取值来源不一致（HTML解析 vs 马匹历史页面） | 需评估两个数据源的权威性差异 |
| 🟡 中 | 增强 | 赔率走势评分梯度加密（当前5档 → 7-8档） | 等用户提供具体设计方案 |
| 🟡 中 | 增强 | 骑师/练马师引入 HKJC 全局胜率横向基准 | 需确认 HKJC 数据可行性 |
| 🟢 低 | 增强 | 贴士指数按场地/距离匹配对应场次 | 当前贴士指数数据源不稳定，暂不处理 |

---

## v1.4.6 — 2026-04-03

### 📋 后备马信息解析与展示

**背景**：香港赛马赛事通常包含 12 匹正选马 + 2 匹后备马（Emergency），正选退赛时由后备递补。

#### 解析层（`parse.py`）
- `parse_race_entries()` 改为保留后备马，通过 `is_reserve=True` 标记（原版直接过滤）
- 新增 `validate_race_entries()` 函数：验证解析结果完整性（数量/字段/马号/评分范围）
- `barrier` 解析增强：改用 `re.sub(r'[^\d]', '', ...)` 兼容含特殊字符的档位值

#### 业务层（`main.py`）
- 新增正选马 / 后备马分离逻辑，班次推断和历史抓取仅针对正选马
- 后备马不参与评分，仅作替补递补信息参考

#### 展示层（`output.py`）
- `format_markdown_output()` 新增 `reserve_horses` 参数，输出后备马信息表格
- 显示后备马马号、马名、评分、练马师，附注说明不参与预测评分

**效果**：后备马信息现已在分析报告中可见，防止遗漏替补情况。

---

## v1.4.5 — 2026-04-03

### 🚀 性能优化：Playwright 单例复用 + 并行历史抓取

#### Playwright 单例管理器（`fetch.py`）
- 新增 `PlaywrightManager` 类，全局复用浏览器实例，避免每次抓取重复启动/关闭 Chromium
- 进程退出时通过 `atexit` 自动清理资源
- `fetch_url()` 新增 `max_retries` 参数（默认3次），支持指数退避重试（1s/2s/4s）
- 新增 `import atexit` 依赖

#### 并行历史战绩抓取（`main.py`）
- 新增 `fetch_all_horse_history()` 函数，使用 `ThreadPoolExecutor` 并行抓取（默认8线程），大幅缩短预测总耗时
- 新增 `_fetch_single_horse_history()` 单匹马抓取包装函数
- 抓取进度实时打印（含马名、历史场数、当前评分）
- 新增 `import time` / `from concurrent.futures import ThreadPoolExecutor, as_completed`

#### 中文场地条件支持（`main.py`）
- 新增 `normalize_condition()` 函数：`--condition` 参数现支持中文（快/好地快/好/略黏/黏）
- 原 `choices=["fast", ...]` 限制移除，改为自由字符串输入（中文/英文均可）

#### 动态班次区间推断（`main.py`）
- 新增 `infer_class_range()` 函数：根据全场参赛马评分分布自动推断班次区间
- 替代原有硬编码默认值，`class_ceiling`/`class_floor` 更贴近实际赛事班次
- 已知问题「班次区间未按实际班次自动设置」已修复

**效果**：历史抓取时间预计从 ~120s（顺序）降至 ~20s（8线程并行）

---

## v1.4.4 — 2026-04-02

### 💰 投注建议模块重构（AI 自主判断）

**背景**：原有投注建议仅为固定格式输出（独赢/连赢/三重彩），缺少判断逻辑和各投注类型的完整说明。

**变更内容**：

- `SKILL.md` 新增完整投注推荐逻辑（Step 4B/4C/4D）：
  - 独赢（Win）：Top1 概率 >= 30% 或赔率 <= 4 倍时推荐
  - 位置（Place）：Top3 任一匹概率 >= 18% 时推荐
  - 连赢（Quinella）：Top1+Top2 概率和 >= 50% 且差距 <= 20% 时推荐
  - 位置连赢（PQ）：Top3 概率分散（差值 > 25%）时推荐三组全包
  - 三重彩（Trio）：Top3 均 >= 10% 且和 >= 45% 时推荐
  - **最推荐方式**：根据概率分布特征自动判断最佳投注类型

- `SKILL.md` 同步修正：
  - 回测自动化时间 `09:00 → 23:30`（中英文版）
  - 专家预测权重 `5-10% → 4%`

---

## v1.4.3 — 2026-04-02

### 🧬 进化建议全量应用（基于 2026/04/01 沙田回测）

**背景**：回测显示独赢命中率 11.1%（基准 15%），前3命中率 29.6%（基准 33%），自动生成 5 条进化建议，经用户确认后全量应用。

#### 建议 1 ✅：Softmax 温度参数调整
- `config.py` → `SOFTMAX_TEMPERATURE`: **1.5 → 2.0**
- 效果：降低极端预测频率，使概率分布更均匀

#### 建议 2 ✅：权重调整——降低历史偏执，提高市场信号
- `config.py` → `DEFAULT_WEIGHTS`:
  - `history_same_condition`: **0.18 → 0.16**（避免历史过拟合）
  - `odds_drift`: **0.13 → 0.15**（更多参考资金流向）

#### 建议 3 ✅：赔率时效性确认（逻辑审查）
- 确认赔率缓存 TTL=5分钟，已属合理，无需修改
- 时间衰减问题由建议 5 解决

#### 建议 4 ✅：配速权重临时降权
- `config.py` → `DEFAULT_WEIGHTS`:
  - `sectional`: **0.15 → 0.10**（临时，待配速实测数据接入后恢复）
  - `history_same_venue`: **0.13 → 0.18**（补充配速释放的 5%）

#### 建议 5 ✅：历史战绩时间衰减加权（新特性）
- `scoring.py` → 新增 `_time_weight()` 函数
- `score_history_same_condition()` 和 `score_history_same_venue()` 引入时间衰减：
  - 近 30 天 × 1.0，31-90 天 × 0.8，91-180 天 × 0.6，>180 天 × 0.4
- 兼容无 `date` 字段的历史记录（默认系数 0.7）

**同步更新**：`references/analysis_weights.md`（权重表 + 修订记录）

---

## v1.4.2 — 2026-04-01

### 🕐 定时任务调整：回测时间改为 23:30

**背景**：原 `09-00` 自动化任务在早上 9:00 运行回测，但此时沙田日间赛尚未开始（约 13:00 开跑），无法抓取赛果。

**变更**：
- 任务 ID 保留 `09-00`，名称改为「赛马回测+进化分析 — 每日23:30」
- 执行时间：每天 **23:30**（原 09:00）
- 覆盖场景：沙田日间赛、黄昏赛、跑马地夜赛（通常 22:30-23:00 结束）

### 📦 新增：预测存档归档隔离机制

**背景**：回测完成后，原预测存档仍留在 `.archive/` 目录，可能被后续回测任务重复加载或污染。

**变更**：
- 新增 `COMPLETED_DIR = .archive/completed/` 目录
- 回测完成后，预测存档自动从 `.archive/` 移至 `.archive/completed/`
- `load_prediction_archive()` 优先从原位置加载，若已归档则从 completed 目录加载
- 防止已完成的预测存档被重复回测

**代码变更**：
- `daily_scheduler.py`：新增 `_archive_completed_prediction()` 归档函数

---

## v1.4.1 — 2026-03-31

### 📝 文档更新：SKILL 贴士指数说明完善

**背景**：用户指出 SKILL.md 中贴士指数的内容不够明确。

**变更**：
- **SKILL.md**：在「辅助分析维度」表格中新增 HKJC 贴士指数行
  - 数据来源：HKJC 官方贴士指数页面
  - 分析要点：官方每日贴士（>100 偏热，<100 偏淡）
  - 权重：6%
- **SKILL_EN.md**：同步更新英文版

**原因**：贴士指数作为 HKJC 官方数据源，是预测的重要参考维度，应在 Skill 文档中明确标注定位和权重，与 `references/expert_sources.md` 中的详细规则保持一致。

### 🏗️ 重构：模块化拆分

**背景**：`analyze_race.py` 代码量达到 1778 行（54KB），功能耦合严重，不利于后期维护和功能扩展。

**重构方案**：将单文件拆分为 9 个模块，各司其职：

| 模块 | 文件 | 职责 |
|:-----|:-----|:-----|
| 配置 | `config.py` | URL常量、缓存TTL、权重默认值、场地/状况映射 |
| 权重 | `weights.py` | 场景/场地/距离适配的动态权重计算 |
| 概率 | `probability.py` | Softmax归一化概率计算 |
| 评分 | `scoring.py` | 所有评分函数（历史/赔率/配速/骑师/贴士等） |
| 缓存 | `cache.py` | 磁盘缓存读写、TTL过期、统计清理 |
| 抓取 | `fetch.py` | HTTP请求、Playwright动态加载、马匹历史/贴士指数 |
| 解析 | `parse.py` | 排位表、马匹历史战绩HTML解析 |
| 分析 | `analyze.py` | 单匹马多维度综合评分 |
| 输出 | `output.py` | Markdown报告格式化 |
| 入口 | `main.py` | CLI解析、主流程编排 |

**向后兼容**：`analyze_race.py` 保留为入口兼容层，直接调用 `main.py`，CLI用法完全不变。

**优势**：
- 每个模块职责单一，便于测试和维护
- 新功能可独立模块开发，不影响其他模块
- 代码复用更清晰（其他脚本可单独 import 某模块）
- 调试时只需关注相关模块，无需阅读全部代码

### 🔧 变更

#### 缓存管理增强
- `config.py` 新增 `tips_index` 缓存类型（30分钟TTL）
- `cache.py` 优化 `_classify_url()` 识别贴士指数URL

---

## v1.3.1 — 2026-03-31

### 🐛 修复

#### 第五轮：贴士指数 JS 动态渲染 + 排位表正则匹配修复
- **问题1**：HKJC 贴士指数页面是 JS 动态渲染，静态 HTML 抓取无法获取完整数据
- **问题2**：排位表页面的 TR class 正则 `class="f_tac f_fs13"` 匹配失败，class 顺序可能是 `f_fs13 f_tac`
- **根因**：
  1. `fetch_tips_index()` 使用 `fetch_url()` 静态抓取，无法获取 JS 渲染后的表格数据
  2. `parse_race_entries()` 正则要求 class 必须为 `f_tac f_fs13` 精确顺序，但实际 HTML 中顺序不固定
- **修复**：
  1. 新增 `fetch_tips_index()` 使用 Playwright 动态加载贴士指数页面，等待 3 秒 JS 执行
  2. 新增 `fetch_url_with_playwright()` 函数用于获取需要 JS 渲染的页面
  3. `main()` 中排位表抓取改用 `fetch_url_with_playwright()`
  4. `parse_race_entries()` 正则改为 `[^\"]*f_tac[^\"]*f_fs13[^\"]*`（同时包含两个 class 即可）
  5. 新增后备马过滤：TD 数 < 12 的行被排除
  6. TD 列索引调整：[0]=马号, [2]=马名, [4]=负磅, [5]=骑师, [6]=档位, [7]=练马师, [10]=评分
  7. 新增 `_fetch_tips_index_static()` 备用解析（Playwright 不可用时）
- **验证**：2026/04/01 沙田第7场成功获取 12 匹马数据 + 12 条贴士指数（#3 銳一=3.0极热门等）
- **贴士指数评分生效**：狼来了 (#3) 贴士3.0 → 评分88；金玉良言(#6) 贴士6.4 → 评分65

#### 第四轮：排序键字段名错误（`final_score` vs `total_score`）
- **问题**：每场预测结果都固定为 `[1, 2, 3]`，无论哪场都一样
- **根因**：`daily_scheduler.py` 中排序键使用 `h.get("final_score", 0)`，
  但 `analyze_race.py` 实际输出的字段名是 `total_score`。
  所有马的 `final_score` 都返回默认值 0 → 得分相同 →
  Python 稳定排序保持原始列表顺序 → 马号 1, 2, 3 永远排在前面
- **修复**：`daily_scheduler.py` 第 266、273 行：`final_score` → `total_score`
- **验证**：修复后各场马号完全多样化，评分差异明显（37–62 分）

#### 第三轮：后备马混入 + 马名提取失败
- **问题**：预测结果混杂后备马（如傳承效應、飛雲），且马号与马名无法对应
- **根因**：
  1. TR 正则 `class="[^"]*f_fs\d+[^"]*"` 同时匹配正选马（`f_tac f_fs13`）和后备马（`bg_white f_tac f_fs13`）
  2. 马名提取正则 `href="...horse\?horseid=[^"]+[^>]*>` 中，`[^"]+` 在 href 闭合 `"` 后遇到紧跟的 `onclick=`（中间无空格），贪心匹配导致失败
- **修复**：
  1. TR 匹配改为精确 `class="f_tac f_fs13"`（排除 bg_white 后备马）
  2. 所有链接提取改用 `[^>]*>` 绕过 onclick 干扰：`r'horseid=([A-Za-z0-9_]+)[^>]*>([^<]+)</a>'`
- **验证**：各场正选马完全独立（无后备），马号与马名一一对应

---

## v1.3.0 — 2026-03-30

### 🆕 新增
- **每日自动化调度系统**（`daily_scheduler.py`）：
  - 每天 14:30 自动检测明天是否为赛马日（HKJC 排位表检测）
  - 若是赛马日，批量预测所有场次并保存预测存档到 `.archive/`
  - 每天 09:00 自动抓取昨日实际赛果，运行回测，输出精度指标
  - 独赢命中率 / 前3命中率 / 场次级别逐场对比
- **自我进化分析引擎**（`daily_scheduler.py` 内置）：
  - 根据回测结果自动识别系统性偏差（高估/低估模式）
  - 生成结构化进化建议（`type` / `priority` / `dimension` / `code_change`）
  - 进化建议仅输出供用户审阅，**不自动修改 Skill**
  - 报告保存到 `.evolution/evolution_YYYY-MM-DD_VENUE.md`
- **进化建议应用工具**（`apply_evolution.py`）：
  - 用户确认某条建议后，安全应用到 `analyze_race.py`（含自动备份）
  - 支持回滚：`--rollback` 还原最近一次改动
  - 应用历史记录：`--history` 查看所有已应用改动
- **预测存档机制**：所有预测结果保存为 JSON，供日后横向对比分析

### 🔧 变更
- `dump_race.py` 硬编码绝对路径 → 改为相对路径（安全审计修复）

### 🏗️ 目录结构新增
```
.archive/     预测存档 + 回测报告 JSON
.evolution/   进化建议报告 MD + 应用历史
.backups/     每次 apply_evolution 的 analyze_race.py 备份
```

---

## v1.2.0 — 2026-03-30

### 🆕 新增
- **骑师动态评分**（`score_jockey`）：不再使用固定值50，改为从该马历史战绩中动态统计：
  - 优先路径：统计「该骑师骑乘本马」的胜率/前3率（≥2场样本）
  - 退化路径：与全场骑师平均前3率对比（1场样本）
  - 兜底：首次骑乘本马返回中性偏低值46；无历史数据返回50
- **练马师动态评分**（`score_trainer`）：逻辑同上，需≥3场样本；换新练马师给42（高不确定性）
- **历史战绩新增骑师/练马师字段**：`parse_horse_history()` 现在从每条历史记录中提取 `jockey`（列10）和 `trainer`（列9）字段，写入 history 结构供评分函数使用

### 🔧 变更
- **权重调整**（释放6%给历史维度，降低骑师/练马师权重）：

| 维度 | 旧权重 | 新权重 | 原因 |
|------|--------|--------|------|
| `history_same_condition` | 15% | **18%** | ↑ 强化数据导向 |
| `history_same_venue` | 10% | **13%** | ↑ 强化数据导向 |
| `jockey` | 8% | **5%** | ↓ 次要因素降权 |
| `trainer` | 7% | **4%** | ↓ 次要因素降权 |

### ✅ 验证
- 实测场次：2026/03/29 沙田第1场（14匹，全缓存秒级完成）
- 骑师分样例：1号部族高手骑师分70（周俊樂1场赔率缩水），11号劍無情骑师分75（艾兆禮高前3率）
- 练马师分样例：10号飛躍凱旋练马师分70（桂福特带本马6场，高前3率）

---

## v1.1.0 — 2026-03-30

### 🆕 新增
- **磁盘缓存系统**：所有 HKJC 数据请求自动缓存到 `.cache/`，按 URL 哈希命名
  - 赛果缓存7天、马匹历史24小时、赔率5分钟，有效避免重复网络请求
  - 新增 `--force-refresh` 参数：强制跳过缓存重新抓取
  - 新增 `--clear-cache` 参数：清除当前场次缓存后重新分析
  - 新增 `--cache-stats` 参数：查看缓存目录统计（文件数/大小/最旧条目）
- **Softmax 概率归一化**：弃用简单线性归一化，改用 Softmax（温度参数 1.5）+单马上限约束（50%），避免极端偏差
- **场景自适应权重**：新增 `--scenario` 参数，支持 `normal/newcomer/class_down/class_up` 四种场景
- **数据充足度标注**：每匹马输出 ⭐/⭐⭐/⭐⭐⭐ 充足度，提示评分可信程度
- **冷门关注逻辑**：同时满足赔率>15、有同条件前3记录、赔率未拉长、班次适配度≥50，自动标记 ⚡ 冷门关注
- **场地/距离/赛道权重调整**：跑马地+3%档位、1800m+长途+5%配速、泥地赛道专项权重
- **中文参数支持**：`--venue` 接受 `沙田/跑马地` 中文输入，自动映射为 `ST/HV`
- **历史战绩精准匹配**：`parse_horse_history()` 实现完整解析，支持途程、场地、班次、走位等字段；同条件容差±200m

### 🔧 变更
- 历史战绩权重拆分：从单一"历史"维度拆为「同距离同场地」+「同场地」+「班次适配度」三个独立子维度
- 赔率权重拆分：从单一"赔率"维度拆为「临场赔率绝对值」+「赔率走势变化」两个子维度
- 跑法推导：从历史走位自动推导马匹跑法（领跑/后追/均速），用于配速×场地状况评分匹配

### ✅ 验证
- 实测场次：2026/03/29 沙田第1场，14匹马历史全部成功抓取
- 同条件评分从默认40提升为实际计算值，数据充足度准确反映历史场次数量
- 冷门关注准确识别3匹马（13号/3号/14号）

---

## v1.0.0 — 2026-03-xx

### 🚀 初始发布

- 基础分析框架：支持 HKJC 赛事数据抓取、多维度评分、预测报告输出
- 支持场地参数 `--venue`、场次 `--race`、日期 `--date`
- 支持 `--output json/markdown` 双格式输出
- 基础评分维度：历史战绩、赔率、配速、骑师（固定50）、练马师（固定50）、档位、专家预测
- Markdown 报告：前3名预测表、全场分项评分表、投注建议

---

## 📌 已知问题 / 待优化

| 优先级 | 问题 | 说明 |
|--------|------|------|
| 🟡 中 | 配速评分仅使用跑法推导，pace_index 无实测数据 | 权重临时降为10%，待接入分段时间数据后恢复 |
| 🟢 低 | 实时赔率接入暂搁置 | HKJC新版网站API路径未稳定，待确认后接入 |
