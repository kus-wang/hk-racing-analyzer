# 2026-04-02 工作日志

## 回测任务成功执行（11:56）

- **日期**：2026/04/01 沙田（9场）
- **独赢命中率**：1/9 (11.1%)
- **前3命中率**：29.6%
- **结果**：进化报告生成 → `.evolution/evolution_2026-04-01_ST.md`
- **预测存档归档**：`.archive/completed/2026-04-01_ST_prediction.json`

## Bug 修复：`_parse_result_html` 解析失败

**根本原因**：
1. **旧缓存**：`.cache/` 中保存了昨天 16:24 抓取的空结果 HTML（赛事当时未结束）
2. **解析正则不匹配**：HKJC 赛果页面的 `<tr>` 结构是多行嵌套，旧正则要求 `<tr><td>名次</td><td>马号</td><td>马名</td>` 在同一行，实际 HTML 中马号在独立多行 `<td>`

**修复方案（daily_scheduler.py `_parse_result_html`）**：
- 改用逐 `<tr>` 解析：提取所有 `<td>` 内容 → 去除子标签 → 按列位置（0=名次/1=马号/2=马名）匹配
- 马名解析：从 `<a>` 标签的文本中提取，去除括号内代码
- 保留旧正则作为方案 B 备用

**额外处理**：清除了 9 个旧的 `LocalResults` 缓存文件（TTL=7天，但内容为空结果页）

## 注意事项（供下次 backtest 参考）
- 如果 backtest 报"解析失败"，优先检查 `.cache/` 中是否有旧的空结果缓存
- 可用命令清除：删除 `.cache/` 中 URL 含 `LocalResults` 的 JSON 文件

## 进化建议全量应用（12:04）

基于 2026/04/01 沙田回测（5条建议全部应用）：

| 建议 | 变更 | 文件 |
|------|------|------|
| 1 | Softmax温度 1.5→2.0 | config.py |
| 2 | history_same_condition 0.18→0.16，odds_drift 0.13→0.15 | config.py |
| 3 | 赔率TTL=5min已合理，逻辑审查无需改动 | — |
| 4 | sectional 0.15→0.10（临时），history_same_venue 0.13→0.18 | config.py |
| 5 | score_history引入时间衰减（30/90/180天分档） | scoring.py |

备份位置：`.backups/config_20260402_120246.py`，`.backups/scoring_20260402_120246.py`
应用历史：`.evolution/applied_history.json`

## SKILL.md / SKILL_EN.md 同步更新（12:15）

- 修正回测自动化任务时间：`09:00 → 23:30`（中英文版均已更新）
- 修正进化建议工作流图描述：`次日 09:00 → 当日 23:30`
- 修正专家预测权重描述：`5-10% → 4%`

## 投注建议模块重构（v1.4.4，12:22）

**判断**：投注建议逻辑属于 AI 推理规则，放在 SKILL.md 更合适；`output.py` 保持纯格式化不变。

SKILL.md 新增内容：
- 5种投注方式（独赢/位置/连赢/位置连赢/三重彩）各自推荐条件
- 最推荐方式判断表（按概率分布特征匹配）
- 输出格式模板

RELEASE_NOTES.md 新增 v1.4.4 条目
